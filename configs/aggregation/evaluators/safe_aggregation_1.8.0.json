{
  "evaluator_id": "safe_aggregation",
  "evaluator_version": "1.8.0",
  "written_on": "2026-08-13",
  "hash_algorithm": "sha256-path-lf-v1",
  "purpose": "The code whose behaviour IS this evaluator. A policy hash says what the conditions were; this says what enforced them. Both are recorded on every aggregation outcome, because the wrong totals this phase found were policies that read correctly and code that did not keep them.",
  "boundary": "Files that can change what the same response becomes, OR whether the verdict may be published. batch_prompt.py stays outside: what is ASKED is versioned by the extraction profile, and folding it in would make every wording change a new evaluator without changing how a response is judged.",
  "versioning": {
    "patch": "logging, wording and provenance only; proven not to move any claim between states",
    "minor": "a new refusal, a safety fix, or any change to scope or set selection",
    "major": "an incompatible input/output contract or a different aggregation algorithm",
    "rule": "If the set of claims landing in derived / rejected / protocol_error / not_applicable can change, it is at least a MINOR — including when the author is sure it is only a bug fix."
  },
  "source_files": {
    "src/react_review/audit/scope.py": "sha256:bee073eef8216f04cf2514dc149b949fdc9fa73f90cc518627be1e79c9ba6e05",
    "src/react_review/normalize/anchors.py": "sha256:c816d4daf0b877c3313ba0e19f67b6708f6366977dc499574a7cb22e05866475",
    "src/react_review/normalize/cohorts.py": "sha256:22c3f111aa4a910be2345285629e26233801d86d0d170a276c88b7dbf3a991a8",
    "src/react_review/normalize/population.py": "sha256:e5f71fa85d68ad283c379872b19154714da64a29c05273e06afd000b3351f150",
    "src/react_review/schemas/batch.py": "sha256:bebdeaef730ec254094135a6a1cc20f5d1bad8e4cec2f016e1a1a1de398444a4",
    "src/react_review/schemas/evidence.py": "sha256:90c72cca69e3655a3bf0548d9418376cc23f7b710b64e762233e1997976385b8",
    "src/react_review/tools/aggregation_identity.py": "sha256:ca2e8291e8ab7a5e3ed9861ecd11a1f08a8f601e31b24cb52f6593513b2e27ae",
    "src/react_review/tools/batch_parse.py": "sha256:1c46c8fede930839a79c2d13212e9fbeb7d65a2a289555d7be442f5c89b39fe0",
    "src/react_review/tools/batch_project.py": "sha256:ccf236eb35ca6dfc37206313a0cf950791846483bc9c10586191a62215edd7d1",
    "src/react_review/tools/batch_result.py": "sha256:b65b7fb46bebaba55dea1765d16d36cf132d8a19707c80b0133f1b1412623ff0",
    "src/react_review/tools/evidence_binding.py": "sha256:dd27bf169d7b97275b677d980b4b9218876cc060a2db1f41d5ee9210ab8665a7",
    "src/react_review/tools/safe_aggregation.py": "sha256:4e09157c1a1aa978300f381109fd15a642b99f7118ec52854f00cc8487b836c1"
  },
  "evaluator_hash": "sha256:2b33656b63734c104f1789ac9ff88907f32ad624772e09b1a14383788c352dfe",
  "supersedes": "1.7.0",
  "why_1_8_0": "Two changes, both MINOR by the rule registered before them.\n\n(1) Attribution fails closed. Two readings reporting the SAME number from the SAME passage were never each other's rivals, because the rival list was built from unequal values, so proximity locked onto the first occurrence — accepting a reading that claimed its neighbour's interval and refusing the one that claimed its own. Wrong in both directions. Neither now receives an interval, and both say why.\n\n(2) A comparison names what it is about. The assigned label read identity.arm_label, which a comparison does not have, so a run whose comparison identity was wrong reported no identity at all — and the gold graded that as an identity released without review. It is now spelled as the single-target route spells it, because both are graded against the same key and a second spelling would be a second answer.\n\nThe 26-case corpus again reported identical and again could not decide this: it exercises the aggregation projector, and neither change is there. The pre-registered rule decides, not the silence.",
  "why_1_7_0": "D1-7.3 verifies each batched reading numeric components against its OWN quote, attributed away from the rival estimates in the same sentence, and maps what survives onto the result. A protocol error rejects only its own entry. The consequence is a NEW REFUSAL: a reading whose quote states an interval the extraction did not return is INCOMPLETE, and an incomplete source may no longer produce a bare MATCH.\n\nThis is MINOR, and the 26-case corpus did NOT decide it. The corpus reported 26 of 26 identical — but it exercises the aggregation projector, and this refusal lives in the comparator, which the corpus does not reach. Silence is not confirmation. The rule registered in PENDING.json BEFORE the change says any new refusal is at least MINOR, and that rule decides it.\n\nKnown boundary gap: audit/compare.py implements the refusal and is NOT inside the hashed boundary, so the evaluator hash does not cover it. Widening the boundary is a governance change of its own and is not made silently here.",
  "why_1_6_2": "D1-6.2 added ExcerptProvenance to schemas/batch.py — which regions of a paper were sent to the model, and by which selector — and repointed tools/aggregation_identity.py at registry_v5. Both files are inside the boundary. Nothing reads the new record back: it is written by the caller that did the windowing and consumed only offline, by an answer key, so no claim can move between derived, rejected, protocol_error or not_applicable because of it. That belief is not what decided the version. eval/aggregation_behavior.py --compare reported 26 of 26 cases identical against the frozen corpus, which is what a PATCH means here.",
  "why_1_6_1": "PATCH, and the word is earned rather than asserted. D1-5 edited three files in this boundary: schemas/batch.py gained the question, binding, projection and execution identities and the persistent reading record; schemas/evidence.py gained batch provenance and the conditional serialisation that keeps it out of legacy artifacts; tools/batch_result.py was untouched in behaviour. eval/aggregation_behavior.py --compare reports 26 of 26 cases identical to the f2adc5e baseline on status, value, chosen object, verified scope, applied axes, component counts and refusal stage. Nothing moved between derived, rejected, protocol_error and not_applicable.",
  "why_1_6_0": "MINOR. The policy and the identity that cleared it are now one object, so a run cannot apply rules readiness never saw; the flag that let a caller declare the scope axes pre-computed is gone, since it skipped the union this policy exists to require; and aggregation_identity.py joins the hashed boundary — it decides whether a verdict may be published, and leaving it out meant changing it and committing produced a clean run under an unchanged evaluator hash.",
  "why_1_5_0": "MINOR, by the rule below: claims move between states. The printed-total route now applies the same axes as the derived one, so an explicit total whose analysis set nobody checked becomes unresolved instead of ok; readiness computes the policy hash itself instead of believing its caller, and covers the files that decide whether a verdict may be published."
}
