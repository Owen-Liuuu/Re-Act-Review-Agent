{
  "pending_id": "d1_7_4_ambiguous_attribution_and_gate_execution",
  "since_commit": "5a8b1ef",
  "reason": "D1-7.4 makes component attribution fail closed when two readings in one batch share a point estimate, so schemas/batch.py and tools/batch_parse.py change again. Both are inside evaluator 1.7.0's hashed boundary.",
  "supersedes_version": "1.7.0",
  "version_rule": "Any new refusal is at least MINOR, whatever the 26-case corpus says — it exercises the aggregation projector and cannot see a comparator or attribution refusal. Ambiguous attribution IS a new refusal, so this is MINOR at least.",
  "on_freeze": [
    "decide and publish the deterministic_compare identity agreed with the reviewer",
    "run eval/aggregation_behavior.py --compare and record the outcome",
    "publish the evaluator, registry, run profile and benchmark profile",
    "pin everything and update tests",
    "delete this file",
    "REPLAY on the frozen commit to produce a release-eligible artifact"
  ],
  "note": "While this file exists no run is release-eligible. The 1.7.0 reanalysis artifact was generated BEFORE its freeze commit and is therefore not release-eligible either; that is why a clean re-replay is on the list."
}
